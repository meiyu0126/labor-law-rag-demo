📂 企業勞基法智慧問答助手 (Labor Law RAG Assistant)
這是一個結合 RAG (Retrieval-Augmented Generation) 技術與 即時網路爬蟲 的 AI 智慧問答系統。 系統預設會自動爬取 全國法規資料庫 的最新勞動基準法條文，確保回答依據是最新的法律規範；同時也支援使用者上傳自定義的 PDF 內部文件（如員工手冊），實現靈活的企業級文件問答。

👉 點擊這裡體驗 Live Demo

🚀專案特色(Key Features)
1. 雙模組資料源 (Hybrid Data Sources)
🕷️ 自動化法規爬蟲：
使用 Requests + BeautifulSoup4 即時抓取全國法規資料庫的勞動基準法。
實作 ETL 清洗，將 HTML 轉換為結構化的 Document 物件（保留條號 Metadata）。
應用 st.cache_data 機制，避免重複請求，提升效能並降低被封鎖風險。
📄 自定義文件上傳：
支援使用者上傳 PDF 檔案（如公司內規）。
使用 tempfile 與 PyPDFLoader 處理暫存與解析，實現即時切換知識庫。
2. 精準 RAG 檢索架構
模型升級：採用 OpenAI text-embedding-3-large 進行高維度向量化，搭配 gpt-4o-mini 進行推論，大幅提升語意理解能力。
智慧來源引用 (Smart Citation)：
針對法規：精準顯示 「第 X 條」。
針對 PDF：精準顯示 「第 X 頁」 與原始檔名。
抗幻覺機制：系統被嚴格限制只能依據「檢索到的內容」回答，若查無資料會如實告知。
🛠️ 技術堆疊 (Tech Stack)
類別,技術/工具,說明:
Frontend,Streamlit,快速建構互動式 Chat UI，整合側邊欄參數設定
Framework,LangChain,使用 LCEL (LangChain Expression Language) 建構 Chain
Model,OpenAI,"GPT-4o-mini (LLM), Text-Embedding-3-Large (Embedding)"
Database,ChromaDB,本地端輕量級向量資料庫 (Vector Store)
Crawler,BS4 & Requests,HTML 解析與資料清洗
Deployment,Streamlit Cloud,CI/CD 自動化部署
📂 專案結構
Plaintext
labor-law-rag-demo/
├── app.py                  # 核心主程式 (包含爬蟲、ETL、RAG Chain、UI 邏輯)
├── requirements.txt        # 專案依賴套件清單
├── .env                    # 環境變數 (OpenAI API Key)
├── data/                   # (Optional) 預設備用 PDF 文件
└── packages.txt            # Streamlit Cloud 系統層級設定
💡 核心邏輯流程 (System Workflow)
1.初始化 (Initialization)：
(1)系統啟動時，檢查是否有上傳檔案。
(2)無上傳：觸發爬蟲 -> 抓取勞動部網頁 -> 解析條文 -> 寫入 ChromaDB。
(3)有上傳：讀取 PDF -> 文字切分 (Chunking) -> 寫入 ChromaDB。
2.檢索 (Retrieval)：
(1)將使用者問題轉為 Vector。
(2)使用 MMR (Maximal Marginal Relevance) 演算法搜尋 Top-K 相關片段，兼顧相關性與多樣性。
3.生成 (Generation)：
(1)將「檢索到的資料」與「歷史對話」填入 Prompt Template。
(2)LLM 生成回答並標註來源。
💻 如何在本地端執行 (Local Installation)
1.Clone 專案
git clone https://github.com/your-username/labor-law-rag-demo.git
cd labor-law-rag-demo
2.安裝依賴套件
pip install -r requirements.txt
3.設定環境變數 建立 .env 檔案，並填入API Key：
OPENAI_API_KEY="sk-proj-xxxxxxxx..."
4.啟動應用程式
streamlit run app.py


